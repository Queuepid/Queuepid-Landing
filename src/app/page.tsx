import LandingHero from '@/components/landing/LandingHero'

// Single-screen landing: animated 3D background, the title card, and the
// controller model loading with its fade-in animations. No scroll.
export default function HomePage() {
  return <LandingHero />
}
